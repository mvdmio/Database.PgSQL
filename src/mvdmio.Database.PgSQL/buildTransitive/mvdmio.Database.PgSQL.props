<Project>
  <!-- Auto-embed schema files from the Schemas directory for transitive consumers -->
  <ItemGroup>
    <EmbeddedResource Include="Schemas\**\*.sql" LogicalName="%(Filename)%(Extension)" />
  </ItemGroup>
</Project>
